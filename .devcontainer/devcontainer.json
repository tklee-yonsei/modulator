{
	"name": "API Server Dev - Coding",
	"build": {
        "dockerfile": "../Dockerfile.vsdev",
        "context": ".."
    },
    "runArgs": [
        "--init",
        "--rm",
        "-v",
        "${localWorkspaceFolder}:/app"
    ],
    "workspaceFolder": "/app"
}
